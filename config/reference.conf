valistrio {
  server: {
    host = 0.0.0.0
    port = 8080

    # Limits the size of any request
    maxBytes = 2097152 # 2MB

    # Limits the time to wait for request body to be received
    requestTimeout = 5s
  }
}
